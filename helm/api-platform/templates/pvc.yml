apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: dev-php-public-media-pvc
  namespace: cobalt
spec:
  volumeName: dev-php-public-media-pv
  accessModes:
    - ReadWriteMany
  volumeMode: Filesystem
  storageClassName: efs-sc
  resources:
    requests:
      storage: 25Gi
