apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: cobalt-pvc
  namespace: kubeflow
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: ""
  resources:
    requests:
      storage: 25Gi
  volumeMode: Filesystem
