apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: php-public-media-pvc
  namespace: cobalt
spec:
  resources:
    requests:
      storage: 25Gi
  storageClassName: efs-sc
  accessModes:
    - ReadWriteMany
  volumeMode: Filesystem
