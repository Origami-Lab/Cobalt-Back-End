apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: php-public-media-pvc
  finalizers: null
spec:
  resources:
    requests:
      storage: 3Gi
  storageClassName: scw-bssd
  accessModes:
    - ReadWriteOnce
  volumeMode: Filesystem
